{
    "foo": "$q{.foo}",
    "bar": "${.foo | .x}",
    "nt": "string",
    "path": "$p{@.foo}",
    "evar": "$e{TEST}",
    "obj": {
      "evar": "$e{TEST}",
      "prefix": "X $e{TEST}",
      "suffix": "$e{TEST} X",
      "embedded": "X $e{TEST} X",
      "multi": "$e{TEST}/$e{TOKEN}"
    }
}

